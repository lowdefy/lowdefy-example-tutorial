For more info see [7. Display ticket data](https://docs.lowdefy.com/tutorial-display-data-page).

Also add a `.env` file with the following:

```
LOWDEFY_SECRET_FILENAME= __ABSOLUTE_PATH_TO_SQLITE_DB_FILE__
```
